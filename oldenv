# Database Configuration
POSTGRES_SERVER=postgres
POSTGRES_USER=voting_user
POSTGRES_PASSWORD=altynHanym27052805altynhanym # <-- ВАЖНО! Измени это!
POSTGRES_DB=voting_app_db
POSTGRES_PORT=5432

DATABASE_URL=postgresql+psycopg2://user:password@postgres:5432/voting_db

# RabbitMQ Configuration
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
RABBITMQ_QUEUE_NAME=vote_queue
RABBITMQ_DLX_EXCHANGE=vote_dlx # Dead Letter Exchange
RABBITMQ_DLQ_QUEUE=vote_dlq   # Dead Letter Queue

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# JWT Configuration (for user identification, worker uses payload token)
JWT_SECRET_KEY=altynHanym27052805altynhanym
JWT_ALGORITHM=HS256
AUDIENCE="fastapi-auth"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Results Cache Configuration
RESULTS_CACHE_TTL_SECONDS=60 # Cache results for 60 seconds

# Worker Specific Configuration
# Seconds to wait before reconnecting to RabbitMQ or DB
WORKER_RECONNECT_DELAY_SECONDS=5
